package com.example.hoaht.extensionfunctionkotlin.model

/**
 * Animal.
 *
 * @author HoaHT
 */
                sealed class Animal

                class Dog:Animal()

                class Pig: Animal(){

                    fun a() {

                        try {
                            // some code
                        } catch (e: Exception) {
                            // handler
                        } finally {
                            // optional finally block
                        }

                    }
                }